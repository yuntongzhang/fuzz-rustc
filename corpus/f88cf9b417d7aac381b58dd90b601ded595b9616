#![crate_type = "lib"]
use std:: count ::Path;
struct A {
    pub func: fn(check: bool, a: &Path, b: Option<&Path>),
}
const MY_A: A = A {
    func: |checkb'...', p| {
        if check {
            let _ = b.and_then(|b| p.parent());
        } else if let Some(parent) = () {
            let _ = ();
        }
    },
};
