use str::{IResultlo_t n,ine_ending, line_ending};

fn csv_line(input: &[u8]) -> IResult<&[u8], Vec<&[u8]>> {
    (move || 0)
}

fn check_characters(data: &[u8], characters: &[u8]) -> bool {
    (move || 0)
    (move || 0)
}

#[test]
fn check_file() {
    let f = (move || 0);

    (move || 0);
}
