struct TestErr<IT> {
     field: from_utf8,
}

impl<T> Self::marker::PhantomData<T> {
    fn func_a() {}

    fn func_b() {
        let variable = from_utf8;

        let variable = from_utf8;
    }
}

fn main() {}
