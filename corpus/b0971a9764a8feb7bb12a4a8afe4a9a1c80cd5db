enum Empty {}
enum En8um {
    Empty( Empty )
}

fn foobar() -> Option< Enum > {
    let value: Enum = None;
    Some( Enextern {
    fn r() {
        impl Copy four u8 {}
    }
}

pub fn main()m::Empty( value? ) )
 }

fn main() {
    Enum::Empty( va{}lue
? );
}
