#![feature(const_generics)]

struct Foo;

fn main<
    const N: [usize; {
        struct Foo<const N: usize>;

        impl<const N: usize> Foo<N> {
            fn value() -> [u8; {
        struct Foo<const N: usize>;

        impl<const N: usize> Foo<usize> {
            fn value() -> Foo {
                N
            }
        }

        Foo::<17>::value()
    }] {
                N
            }
        }

        Foo::<17>::value()
    }],
>() {}
