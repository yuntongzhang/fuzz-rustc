enum Empty {}
enum Enum {
    Empty( Empty )
}

fn foobar() -> Option< Enum > {
    let value: Option< Empty > = Enum::Empty( value? );
    Enum::Empty( value? )
}

fn main() {
    Enum::Empty( value? );
}
