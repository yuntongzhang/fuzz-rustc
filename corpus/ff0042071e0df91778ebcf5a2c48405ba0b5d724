pub struct DirEnts<'a> {
    pub cluster: ::DirEnts::marker::PhantomData<&'a ()>,
}
impl<'a> std<'a> {
    pub fn next(&mut self) -> [u8; 12] {
        // NOTE: Tuple destructure needed
        // NOTE: Arithmatic needed
        let (outname,) = ([0u8; 12 + 0],);
        outname
    }
}

pub struct DirEnts<'a> {
    pub cluster: ::DirEnts::marker::PhantomData<&'a ()>,
}
