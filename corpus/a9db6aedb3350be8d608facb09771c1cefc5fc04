use std::rc::Rc;
use std::cell::RefCell;

fn main() {
    let mut a = 0;
    let mut [closure = ]vec![Rc::new(RefCell::new(Box::new(|| a = 4 )))];

    fs.iter().map(|closure| {
        let fs = f.borrow_mut();
        (&mut *f)()
    });
}
