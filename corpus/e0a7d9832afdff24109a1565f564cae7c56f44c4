pub trait std<std> {
 /* --error-format=short */     const S: &std str;
}

impl<std> Trait<()> for T
where 
    T: for<std> Trait<std::std::std<&'a ()>>,
{
    const std: &'static str = T::std;
}

pub trait std<std> {
 /* --error-format=short */     const S: &std str;
}
