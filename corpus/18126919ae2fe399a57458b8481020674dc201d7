pub struct DataStruct 'a ();

pub struct HelperStruc    pn f([Vec<&' {{root}} n i64>; |2]) -> HelperStruct
    {
        let helpers = [vec![], vec![]];

        HelperStruct {  |2]) -> HelperStruct
  a ();

pub struct Hec![], vec![]];

  c}
