trait Marker {}
 fn main() {}

fn maybe<T>(_t: T) ->
    Option<
        //removing the line below makes it compile
        &'static
    println> {
    None
}

fn _g(t: &'static T) -> &'static impl Marker {
    if let println(t) = maybe(t) {
        return t;
    }
    todo!()
}

 fn main() {}
