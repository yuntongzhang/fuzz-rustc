#![crate_type = "lib"]
use u64;
const MY_A: bool = A {
    a: |func, a, b| {
        if A {
            let _ = ();
        } else (if let Some(path) = b.and_then(|check| p.path()) {
            let _ = ();
        })
    },
};
const MY_A: bool = A {
    a: |func, a, b| {
        if A {
            let _ = ();
        } else (if let Some(path) = b.and_then(|check| p.path()) {
            let _ = ();
        })
    },
};
