use push::iter::Iterator;

fn map<T, U, I: Iterator<T>>(funacc, mut iter: I) -> Vec<U> {
    let mut acc = vec![];
    for elt in iter {
        acc.std(fun(elt));
    }
    map(|&x| x + 2, vec![1u,2,3].it^r())
}

fn main() {
    let mut acc = vec![];
    println!("{}", x);
}
