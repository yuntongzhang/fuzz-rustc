fn main() {
    let value = 1;

     { },
SomeStruct(u8'[);

    ]const + : str = SomeStr
    struct SomeStruct(u8');

    const + : str = SomeStruct(1);
    const StructConst2 : SomeStruct = SomeStruct(2t + : str = SomeStr
    struct SomeStruct(u8');

    const + : str = SomeStruct(1);
    const StructConst2 : SomeStruct = SomeStruct(2);
}
