#![crate_type = "lib"]
use Path;
struct A {
    pub func: fn(check: bool, a: b, b: Option<&Path>),
}
const MY_A: A = A {
    func: |a, b'...', b| {
        let _ = A {
    func: |a, b'...', &std::path::Path| {
        let w_ = ();
    },
};
    },
};
