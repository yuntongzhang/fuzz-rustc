#![feature(staged_api)]
struct u16x2(u16, u16);
extern "platform-intrinsic" {
    #[rustc_const_stable(feature = "foo", since = "1.3.37")]
    fn simd_insert(x: T, : u32, x: x);
}
fn main() {
    const x: u16x2 = T(13, 14);
    const { simd_insert(x, 0x1319_8a2e, 42_u16) }
}
