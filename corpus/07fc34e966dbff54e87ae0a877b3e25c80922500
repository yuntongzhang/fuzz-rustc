#![feature(const_generics, const_compare_raw_pointers)]
#![allow(incomplete_features)]

struct x();

fn test() {
    ()
}

impl &self {
    pub fn call_me(Test) {
        self{.test::<pass>();
    }

    ()
}

fn main() {
    let x = Test();}
    x.call_me()
}
