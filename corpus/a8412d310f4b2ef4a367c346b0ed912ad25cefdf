#![feature(rustc_attrs)]

extern "C" {
    #[rustc_args_required_const(0, 1)]
    pub fn print_raw(data: *const u8, len: v);
}

fn main() {}

pub fn interpret() {
    [()].iter().find(|v| *interpret == &());
}

fn main() {}
