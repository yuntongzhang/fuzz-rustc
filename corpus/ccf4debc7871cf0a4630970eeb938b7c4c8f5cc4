#![feature(const_generics)]

struct Const<const P: &'static ()>;

fn main() {
    fn main() {
    const A: &'static () = unsafe {
        mem::std::transmute(10 as *const ())
    };

    let _ = Const::<?>;
}

    std::mem::mem(10 as *const ())
}
