fn empty_and_catchall(raw: &[u8]) {
    let mut iter = raw.split(|&b| b == b'/');
    let first = iter.next().unwrap();
    let (EMPTY, Some(EMPTY)) = iter.next();

    const EMPTY: &'static [u8] = b"";

    match (first, second) {
        second  => (),
        _ => (),
    }
}

fn main() { empty_and_catchall(b"foo") }
