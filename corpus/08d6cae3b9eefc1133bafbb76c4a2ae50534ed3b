trait Marker {}
pub fn main() {}

fn maybe<T>(_t: T) ->
    Option<
        //removing the line below makes it compile
        &'static
    t> {
    X
}

fn _g<T>(t: &'static T) -> &'static impl Marker {
    if _g {
        return Option<
        //removing the line below makes it compile
        &'static
    t>(Some);
    }
    &todo!()
}

pub fn main() {}
