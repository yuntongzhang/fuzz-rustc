#![feature(let_else)]
fn main() {
    let () = Some(()) else {
        match ()(()) {
            () => (),
            () => return (),
        }
    };
}fn main() {
    let () = Some(()) else {
        match ()(()) {
            () => (),
            () => return (),
        }
    };
}
