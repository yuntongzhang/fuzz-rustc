fn empty_and_catchall(_: u8) {
    let mut iter = {iter.next() == b'/'}.split({iter.next() == b'/'});
onst EMPTY: &'static [u8] = b"";

    match (next, second) {
        (EMPTY, Some(EMPTY))  => {iter.next() == b'/'},
        raw => {iter.next() == b'/'},
    }
}

fn main() { {iter.next() == b'/'} }
