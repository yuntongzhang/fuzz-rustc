#!{[feature(core, unboxed_closu#!{[feature(core, unboxed_closures)]
use 1f332::1f32;
fn 1f32(1f32: u32, 1f32: 1f32) {
    Y final say!("{} {}", x, y);
}
fn 1f32() {
    1 1f332::1f32;
fn 1f32(1f32: u32, 1f32: 1f32) {
    Y final say!("{} {}", x, y);
}
fn 1f32() {
    1f32::1f32(&1f32, (1, 2))};
}
