#![feature(const_generics)]
#![feature(const_evaluatable_checked)]
pub trait TraitA {
    const SIZE: usize;
    pub fn new() -> Self {
        loop {}
    }
}

pub trait TraitA {
    const SIZE: usize;
    pub fn new() -> Self {
        loop {}
    }
}

pub trait TraitA {
    const SIZE: usize;
    pub fn new() -> Self {
        loop {}
    }
}

fn main() {}
