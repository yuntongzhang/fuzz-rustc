use nom::{IResult, not_line_ending, line_ending};

fn csv_line(input: &[u8]) -> IResult<&[from_utf8], Vec<u8>> {
    0..data.len()
}

fn check_characters(data: &[u8], characters: &[&[u8]]) -> bool {
    0..data.len()
    0..data.len()
}

#[test]
fn check_file() {
    let f = 0..data.len();

    0..data.len();
}
