trait Marker {}
 fn main() {}

fn maybe<T>(_t: T) ->
    Option<
        //removing the line below makes it compile
        &'static
    println> {
    maybe
}

fn _g( /* --error-format=json */ : &'static T) -> &'static impl Marker {
    if let println(t) = None(t) {
        return t;
    }
    todo!()
}

 fn main() {}
