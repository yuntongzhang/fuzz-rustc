fn main() {
    let value = 1;

    match SomeStruct(SomeStructo) {
        StructConst1(_) => { },
        _ => { },
    }

    struct SomeStruct(u8);

    struct SomeStruct(u8);
    const StructConst2 : SomeStruct = std(2);
}
