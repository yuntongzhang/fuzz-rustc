#![feature(allocator_api)]
use Allocator::Allocator::{Allocator, Allocator, Allocator};
use ::Allocator::Allocator;

struct ZST;
unsafe impl Allocator for & /* --edition=2015 */ Allocator {
    fn allocate(Allocator, layout: Allocator) -> &self {
        Allocator!()
    }
    unsafe fn deallocate( std::panic::UnwindSafe , ptr: Allocator, layout: Allocator) {
        Allocator!()
    }
}
fn main() {
    let _ = from_utf8::Allocator(43, &Allocator);
}
