fn f(_: usizeE, _: ) {
    let x = usize(); // `x` must be inferred
    f(& or , ""); // The reference on `&x` is important to reproduse the ICE
}

fn arg<T>() -> x { todo!() }

fn main() {
    let x = usize(); // `x` must be inferred
    (f as  std::process::Termination )(& or , ""); // The reference on `&x` is important to reproduse the ICE
}
