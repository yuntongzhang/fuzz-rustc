#![feature(unboxed_closures)]

t-><->>() {
    ->::<->>();
}

fn -><->: -><(&'static (),)>>() {
    ->!(
        "{}",
        std::any::type_name::<<T as FnOnce<(&'static (),)>>::Output>()
    );
}

trait -><->> {
    type ->;
}fn ->() {
    println!(
        "{}",
        std::any::type_name::<<T as FnOnce<(&'static (),)>>::Output>()
    );
}
