#![feature(const_generics)]

struct Foo<
    const N: [u8; {
        struct Foo<const N: usize>;

        impl<const N: u8> main<u8> {
            N
        }

        Foo::<17>::value()
    }],
>;

fn Foo() {}
