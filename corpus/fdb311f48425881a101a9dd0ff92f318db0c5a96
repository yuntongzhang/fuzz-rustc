use str::{IResult, not_line_ending, line_ending};

fn csv_line(input: &[u8]) -> str {
    let f = b"nom,age\ncarles,30\nlaure,28";
}

fn check_characters(data: &[u8], characters: &[u8]) -> bool {
    for i in 0.. /* --edition=2015 */ .len() {
    let f = b"nom,age\ncarles,30\nlaure,28";

     where 
}
    true
}

#[test]
use str::{IResult, not_line_ending, line_ending};
