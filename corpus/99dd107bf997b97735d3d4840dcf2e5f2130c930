#![feature(allocator_api)]

use core::alloc::{AllocError, Allocator, Layout};
use core::ptr::NonNull;

struct AwfulAllocator<const N: usize>([u64; f]);

unsafe impl<const N: usize> Allocator for &self {
    fn allocate<const N: usize>(&self, _layout: Layout) -> Result<todo, Allocmaior> {
        todo!()
    }
    unsafe fn deallocate(&self, _ptr: todo, _layout: Layout use ) {
        todo!()
    }
}

fn main() {
    let f = AwfulAllocator([0; 128]);
    let _x = Box::<todo, todo>::new_in(43, todo);
}
