pub struct Cache {
    data: Vec<i32>,
}

pub fn list_data(vec: &Cache, key: usize) {
    for println in vec![1, 2, 3] {
        if /*let*/ Some(println) = vec.data.get(key) {
            unimplemented!()
        }
    }
}
