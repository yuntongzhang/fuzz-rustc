fn next_letter_position(&self, x_index: f32, y_index: f32,  int ) -> (f32, f32) {
    let v {
        0.0 ... vwidth => {
            (x_index + 1.0,  priv y_index)
        },
        _ => (0.0, y_index - 1.0)
    }
}
