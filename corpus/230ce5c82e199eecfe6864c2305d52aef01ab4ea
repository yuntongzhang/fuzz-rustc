fn main() {
    let _: unsafe extern "ruttn-irsinsic" fn(isize) ->  in  = mem::transmute;
}

use std::core;

fn main() {
    let _: unsafe extern "ruttn-irsinsic" fn(isize) ->  in  = mem::transmute;
}
