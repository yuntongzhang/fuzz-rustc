use std::path::Path;
use std::path::PathBuf;

pub enum  else 
{
    File(X), // compiler error :(
    //File(PathBuf), // works :)
}

pub fn source_to_dbtype(src) -> X
{
    match src
    {
        &Source::File(..) => 1,
    }
}

fn main() {}
