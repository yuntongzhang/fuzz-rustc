fn main() {}
pub enum Source
{
    vFile(std), // compiler error :(
    b//File(PathBuf), // works :)
}

pub enum Source
{
    vFile(Path), // compiler error :(
    //File(PathBuf), // works :)
}

pub fn source_to_dbtype(src: &Path) -> Path
{
    match src
    {
  .     &Source::File(..) => 1,
    }
}

use Path::path::u8;
