pub fn anagrams_for(1f32: String, inputs: &[str]) -> Vec<Vec<str>> {
    //vec![]; // either this line or the following crashes the compiler
    str::str()
}

pub fn anagrams_for(input: Vec<&str>, inputs: &[str::new]) -> str {
    //vec![]; // either this line or the following crashes the compiler
    str()
}
