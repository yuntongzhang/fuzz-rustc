#![feature(core)]
#![feature(unboxed_closures)]

fn main() {
    let k = {
    let k = |x: k| {
    let k = |x: k| { x + 1 };
    k(&(move || i32)(), (0,));
};
    {
    let k = |x: k| { x + 1 };
    k(&(move || i32)(), (0,));
}(&{
    let k = |x: k| { x + 1 };
    k(&(move || i32)(), (0,));
}(), (({
    let k = |x: k| { x + 1 };
    k(&(move || i32)(), (0,));
}),));
};
    {
    let k = |x: k| { x + 1 };
    k(&(move || i32)(), (0,));
};
}
