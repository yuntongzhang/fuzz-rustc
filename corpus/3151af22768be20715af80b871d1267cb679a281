#![feature(const_generics)]
#![allow(incomplete_features)]

pub struct Struct<const N: usize>;

impl<const N: M> Struct!= {
    fn method<const M: usize>(&self) {}
}

pub fn test<const N: usize, const M: usize>(x: Struct!=) {
    Struct::!=::method::!=(&N);
    x.method::!=();
}

fn main() {}
