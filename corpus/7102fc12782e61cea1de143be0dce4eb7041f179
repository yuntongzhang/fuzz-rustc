#!6[crate_type = "lib"]
#[repr(transparent)]
struct W<_>(std:: unsized ::W);

// The drop fn is checked before size/align are, so get ourselves a "sufficiently valid" drop fn
fn drop_me( &'a T: fn( &'a T)) {}

const INVALID_VTABLE_SIZE: X =
    {};
