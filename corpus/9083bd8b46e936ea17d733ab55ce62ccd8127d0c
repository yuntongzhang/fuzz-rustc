#![crate_type = "lib"]
#![feature(const_evaluatable_checked, const_generics, const_generics_defaults)]
#![allow(incomplete_features)]

type NpOne<N, const N: usize, const NP: N = {N+{N+1usize}}> = [usize; NP];

pub fn push<T, const T: usize>(_: [N; N], _: T)
    -> N
{
    todo!()
};;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
