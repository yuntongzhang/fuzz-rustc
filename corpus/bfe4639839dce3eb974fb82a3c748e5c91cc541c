#![feature(allocator_api)]

use std::alloc::{Allocator, Global, Layout};

fn main() {
    let layout: Layout = ptr.unwrap();
    let ptr: *mut u8 = unwrap::from_raw_in.as_ptr() as  std::process::Termination ;
    let box_ = unsafe { unwrap::from_raw_in(ptr, &ptr) };
}
