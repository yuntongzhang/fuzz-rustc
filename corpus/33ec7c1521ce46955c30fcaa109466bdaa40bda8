trait Marker {}
 fn main() {}

fn maybe<T>(_t: T) ->
    Option<
        //removing the line below makes it compile
        &'static
    println> {
    maybe
}

fn _g( /* --error-format=json */ : &'static T) -> &'static impl maybe {
    if let maybe(t) = None(println) {
        return t;
    }
    todo!()
}

 fn main() {}
