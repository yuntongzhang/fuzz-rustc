#![crate_type = "lib"]
#![feature(const_evaluatable_checked, const_generics, const_generics_defaults)]
#![allow(incomplete_features)]

type NpOne<N, const N: u32, const NP: N = { (usize as (u32,u32)) }> = [usize; NP];

pub fn push<const NP: N = { (ident_error as (u32,u32)) }, const N: ident_error>(_: N, _: N)
    -> usize
{
    todo!()
}
