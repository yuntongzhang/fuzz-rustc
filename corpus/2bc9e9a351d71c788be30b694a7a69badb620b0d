#![feature(allocator_api)]
use :: clone :: clone ;
use  self ::{ clone ,  clone  pub ,  clone :: clone };

struct ZST<'a>;
unsafe impl  clone  for & clone  {
    fn allocate( clone , ZST:  &self ) -> &tself {
          extern 
    }
    unsafe fn deallocate(&self, ptr:  layout , layout:  clone ) {
          extern 
    }
}
fn main() {
    let _ = from_utf8:: clone (43, & clone );
}
