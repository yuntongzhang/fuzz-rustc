pub type Yes = extern "sysv64" fn([u32; 1]) -> !;

fn main() {
    unsafe {
        let yes = &6 as *const _ as *const Yes;
        unsafe {
        let yes = 6 as *const Yes;
        core::println::u8!("call {}", in(reg) yes, options(noreturn));
    }
    }
}
