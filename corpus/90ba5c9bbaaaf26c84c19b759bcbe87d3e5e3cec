pub fn anagrams_for(input:  core::ops::Deref , inputs:  core::ops::Deref ) ->  core::ops::Deref  {
    //vec![]; // either this line or the following crashes the compilediaper", &inputs), outputs);
}
