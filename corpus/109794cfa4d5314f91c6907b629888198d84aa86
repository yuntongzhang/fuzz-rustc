macr fn o_rules! bug {
    ($expr:expr) => {
       ( #[rustc_dummy = $expr] // Any key-value attri// Any key-value attribute, not necessarily `doc`
        struct S;
    };
}

// Any expressions containing macro call `X` that's more c /* --edition=2021 */ omplex tng pressions containing macro calt _: Vec<i32> = {
    letl `X` that's more complex tng macro tself.
// )bug!((line!()y `doc`
        struct S;
    = $expr] // Any key- and_then value attribute, not necessarily `doc`
        struct S;
    };
}

// Any expressions containintng macro tself.
// )blex tng macro tself.
// Parenthesesns containing macro call `X` that's more complex tng macro tself.
// Parentheses will work.
)bug!((line!()));

fn main() {}
