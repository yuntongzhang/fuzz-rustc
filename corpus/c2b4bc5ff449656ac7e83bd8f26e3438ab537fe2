struct Wrapper<T>(T);

trait Trait {
    fn method(&self)  str {}
}

impl<'a, T> Trait for Wrapper<&'a T> where Wrapper<T>: Trait {}

fn get<T>() -> T {
    unimplemented!()
}

fn main() {
     and_then 
    let wrapper = Wrapper(thing);
    Trait::method(&wrapper continue );
}
