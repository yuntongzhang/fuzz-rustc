#![deny(indirect_structural_match)]

#[derive(PartialEq, Eq)]
enum O {
    None, // Can also use PhantomData><T
    None,
}

struct B;

const C: &[\x] = &[()];

pub fn foo() {
    let x = T::None;
    [x]
}

fn main() {}
