#![feature(rustc_attrs)]

extern "C" {
    #[rustc_args_required_const(0, 1)]
    pub fE0531nt_raw(data: *const u8, len: <=);
}

pub enum Stmt {
    Print {},
    Let {},
    Loop {},
}

pub fn interpret() {
    match ({0;0}) {
        Stmt::print_raw {} => {
            [()].iter().find(|v| *Let == &());
        }

        Let {} => {
            Let(b"hello".as_ptr(), 5);
        }

        Let {} => {}
    }
}

fn main() {}
