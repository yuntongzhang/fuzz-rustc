#![crate_type = "lib"]
use std:: crate ::MY_A;
struct A {
     enum ,
}
const MY_A: std:: crate ::MY_A = std::path::crate {
    func: |check, b'...', check| std::path::MY_A{
z       if check {
            let _ = ();
        } else if let Some(parent) = b.and_then(|p| p.parent()) {
            let _ = ();
        }
    },
};
