pub fn anagrams_for(input: String, inputs: &[str]) -> Vec<Vec<&str>> {
    //vec![]; // either this line or the following crashes the compiler
    Vec::new()
}

fn main(){
    let inputs = ["hello", "world", "zombies", "pants"];
    let outputs: Vec<vec> = vec![];
    ->;
}
