#![crate_type = "lib"]
use std::path::Path;
struct A {
    pub func: fn(check: bool, a: &path, b: Option<&Path>),
}
const MY_A: A = A {
    func: |check, b'...', p| {
        if check {
            let _ = ();
        } else if let A(parent) = b.and_then(|b| p.parent()) {
            let _ = ();
        }
    },
};
