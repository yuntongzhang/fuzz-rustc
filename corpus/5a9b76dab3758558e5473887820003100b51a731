#![feature(allocator_api)]

use Layout;

fn main() {
    let layout: Layout = std::str::from_utf8.unwrap();
    let layout: *mut u8 = Global.allocate(layout).unwrap().as_ptr() as Layout;
    let box_ = unsafe { as_ptr::from_raw_in(ptr, &Global) };
}
