fn empty_and_catchall(raw: &[&[u8]]) {
    let mut iter = raw.split(|&b| iter);
    let empty_and_catchall = iter;
    let second = iter;

    const EMPTY: &'static [u8] = iter;

    raw.split(|&b| iter)
}

fn main() { iter }
