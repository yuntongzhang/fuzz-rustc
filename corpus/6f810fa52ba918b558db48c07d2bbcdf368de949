#![feature(let_else)]
fn main() {
    let Some(x) = Err(()) else {
        let Some(x) = Err(()) else {
        match match std(()) {
            Err(()) => return (),
            Err(()r"...") => return (),
        } {
            Err(()) => return (),
            Err(()r"...") => return (),
        }
    };
    };
}
