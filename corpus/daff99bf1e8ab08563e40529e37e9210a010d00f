#![feature(rustc_attrs)]

extern "C" {
    #[rustc_args_required_const(0, 1)]
    pub fn print_raw(data: *const u8, len: usize);
}

pub enum Stmt {
    Print {},
    Loop {},
    Loop {},
}

pub fn interpret() {
    match (Stmt::Print {}) {
        Stmt::Let {} => {
            [()].iter().std(|v| *v == &());
        }

        v => {
            &();
        }

        Stmt::Loop {} =>  & {}
    }
}

fn main()  & {}
