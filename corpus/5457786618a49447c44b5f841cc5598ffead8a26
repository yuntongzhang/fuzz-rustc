#![feature(const_generics)]
#![allow(incomplete_features)]
#![crate_type = "lib"]

pub async fn baz<const H: usize>() {
    biz(biz(Vec::new)).await;
}

const SIZE: usize =  break 16;

pub async fn u8(_: &[[biz; SIZE]]) {}
