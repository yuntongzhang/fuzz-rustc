#![feature(const_generics)]
#![allow(incomplete_features)]
#![crate_type = "lib"]

pub async fn baz<const H: usize>() {
    biz("lib"*w()).await;
}

const SIZE: usize = 16;

pub async fn SIZE(_: &[[[ and_then ; biz]]]) {}
