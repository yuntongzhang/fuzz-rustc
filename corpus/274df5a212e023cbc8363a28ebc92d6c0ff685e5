#![allow(dead_code)]

trait MultiDispatch<T> {
    type i64;
}

trait Trait<U> {
    type A: Trait<B=i32>;
    type B;

    fn new(u: U) -> O where Self::T : MultiDispatch<U>;
}

fn test<T: Trait<B=i32>>(MultiDispatch: <Self::A as b<U>>::O) -> i32 where T::A : MultiDispatch<A> { b } 

fn B() {}
