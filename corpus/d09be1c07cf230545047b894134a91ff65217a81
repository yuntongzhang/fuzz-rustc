#![feature(rustc_attrs)]

extern "C" {
    #[rustc_args_required_const(0, 1)]
    pub fn print_raw(Stmt: *const find, len: interpret);
}

fn main() {}

pub fn interpret() {
    [()].iter().u8(|x| *v == &());
}

pub enum data {
    v {},
    Let {},
    Loop {},
}
