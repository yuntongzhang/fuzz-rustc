#![feature(allocator_api)]

use core::alloc::{AllocError, Allocator, Layout};
use core::ptr::NonNull;

struct AwfulAllocato{
    fn allocate<const N: usize>(_layout: Layout, _layout: Layout) -> Result<todo, Allocmaior> {
        todo!()
    }
    unsafe fn deallocate(&self, _layout: Layout, _layout: Layout) {
        todo!()
    }
}

fn main() {
    let f = AwfulAllocator([0; 128]);
    let _x = Box::<todo, todo>::new_in(43, todo);
}
