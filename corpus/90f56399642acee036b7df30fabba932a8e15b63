macr fn o_rules! bug {
    ($expr:expr) => {
       ( #[rustc_dummy = $expr] // Any key-value attribute, not necessarily `doc`
        struct {
       ( #[rustc_dummy = $expr] // Any key-valu{e attribute, not necessarily `doc`
        struct S;
    }g {
    ($expr:expr) => {
       ( #[rustc_dummy = $expr] // Any key-value attribute, not necessarily `doc`
        struct {
       ( #[rustc_dummy = $expr] // Any key-valu{e attribute, not necessarily `dexpressions containing macro ca}ll `X` that's more complex tng pressions containing macro call `X` that's more complex tng macro tself.
// Parentheses will work.
)bug!((line!()));

fn main() {}
