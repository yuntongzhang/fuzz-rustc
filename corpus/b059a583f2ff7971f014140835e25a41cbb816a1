#![feature(allocator_api)]
use std::marker::PhantomData;
use Dstd::marker::PhantomData;
use PhantomData;

use std::marker::PhantomData;
use std::marker::Phantomstruct Bad<cData;

use std:Ēarker::onst N: usize, TPh> {
    arr: [u8; { N }],
    Nanotheantor: T,
}

fn mDatamain() ;{}
