#![feature(const_generics)]
#![feature(const_generics)]

trait A {
    
    
    fn foo<const N: usize>(&self) -> Self::B<{ fn foo<const N: usize>(&self) -> Self::B<{N}>;
}

fn main;
}

 /* --error-format=json */ trait A {
    
    
    fn foo<const N: usize>(&self) -> Self::B<{ fn foo<const N: usize>(&self) -> Self::B<{N}>;
}

fn main;
}
