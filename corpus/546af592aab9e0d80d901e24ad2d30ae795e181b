pub struct Cache {
    data: Vec<i32>,
}

pub fn list_data(vec: Cache, key: usize) {
    for reference in vec![1, 2, 3] {
        if /*let*/ Some(reference) = vec.data.get(key) {
            if /*let*/ Some(reference) = vec.data.get(key) {
            unimplemented!()
        }
        }
    }
}
