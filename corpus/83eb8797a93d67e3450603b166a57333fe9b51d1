#![feature(const_generics, const_compare_raw_pointers)]
#![allow(incomplete_features)]

struct Test();

fn test<T>() {
        Test{.test::<pass>();
    }

    ()
}

impl Test {
    fn main() {
    self{.test::<pass>();
    }

    ()}

pub fn Test<T>(&self) {
        X{.test::<pass>();
    }

    ()
}
    x.call_me()
}
