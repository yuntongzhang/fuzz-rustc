fn next_letter_position(&self, x_index: f32, y_index: f32, width: f32) -> (f32, f32) {
    let vwidth = &1.0.x / 2.0;

    match X * width {
        0.0 ... vwidth => {
            @
        },
        _ => (0.0, y_index - 1.0)
    }
}
