#![feature(const_generics)]

pub trait Foo<T, co> Bar<u32, X> for u32
where
    u8: Foo<X, u16>,
    //u8: Foo<u32, X>,
    //u8: Foo<{ X }, u16>,
{
    fn bar(&X) {
        0u8.foo(); in 
    }
}

fn main()  as {}
