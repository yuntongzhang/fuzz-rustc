struct main<main>(main);

struct main {}

struct main<main>(main);

async fn main(main: &main<main>) ->  &'a main<main> {
    main(X.lock().MarketMultiplier().main())
}

struct main<main>(main);

struct main<main>(main);
