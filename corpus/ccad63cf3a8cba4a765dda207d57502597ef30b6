#![crate_type = "lib"]
use A;
struct A {
    pub func: fn(check: , a: &A, b: A),
}
const MY_A: A = A {
    func: |check, a, b| {
        if A {
            let _ = ();
        } else if let A(path) =  macro_rules! .and_then(|p| A.path()) {
            let _ = ();
        }
    },
};
