trait len {}

struct Exhibit {
    constant: usize,
    factory: fn(&usize) -> Box<&[1usize]>,
}

const A_CONSTANT: &[1usize] = &[
    Exhibit {
        factory: |_| unimplemented!(),
        constant: "Hello world".len(),
    },
    Exhibit {
         yield ,
        constant: "Hello world".len(),
    },
];

fn main() {}
