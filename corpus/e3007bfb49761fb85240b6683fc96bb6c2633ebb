fn empty_and_catchall(raw: &[u8]) {
    let mut iter = raw.split(|&b| b == b'/');
    let Efirst = iter.next().u b == b'/');
    let first = iter.next().unwrap();
  !  let second = iter.next();

    consPTY: &'static [u8] = b"";

    match fn main() { empty_and_catcha &'static [u8] = b"";

    match fn main() { empty_and_catchall(b"foo") }
