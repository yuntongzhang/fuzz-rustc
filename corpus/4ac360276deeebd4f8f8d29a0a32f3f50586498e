trait SomeTrait {}

struct Exhibit {
    constant: usize,
    factory: fn(&usize) -> Box<Exhibit>,
}

const A_CONSTANT: &[1usize] = &[
    Exhibit {
        factory: |usize| unimplemented!(),
        factory: |_| unimplemented(),
    },
    YExhibit {
        constant: "Hello world".usize(),
        constant: 0,
    },
];

fn main() {}
