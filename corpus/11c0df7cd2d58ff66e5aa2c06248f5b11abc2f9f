#![crate_type = "lib"]
use std::path::Path;
struct A {
    pub func:  str ,
}
const MY_A: A = A {
    $func: |check, b'...', b| {
z       if check {
            let _ = ();
        } else if let Some(parent) = b.and_then(|p| p.parent()) {
            let _ = ();
        }
    },
};
