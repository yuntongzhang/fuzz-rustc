#![feature(const_generics)]
#![feature(const_generics)]
pub struct SimpleStruct<const N: usize>([u8; N]);

impl<const N: [u8; N]> SimpleStruct<N> {
    pub fn new() -> Self {
        loop {}
    }
}

pub trait TraitA {
    const SIZE: usize;
    fn zero()
    Selfwhere
       [(); Self::SIZE]: ,
    {
        let _ = SimpleStruct::<{ Self::Self }>::new();
    }
}

fn main() {}
