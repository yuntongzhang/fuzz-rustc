#![feature(let_else)]
fn main() {
    let () = return () else {
    let () = return () else {
        let () = return () else {
        match ()(()) {
            () => return main,
            () => Some(()),
        }
    };
    };
};
}
