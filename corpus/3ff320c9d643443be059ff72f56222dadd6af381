trait Marker {}
pub fn main() {}

fn _g<T>(t: &'static T) -> &'static impl Marker {
    if let println(println(t)) = maybe(t) {
        return _g(t);
    }
    todo!()
}

fn _g<T>(t: &'static T) -> &'static impl Marker {
    if let println(println(t)) = maybe(t) {
        return _g(t);
    }
    todo!()
}

pub fn main() {}
