#![feature(const_discriminant)]
#![feature(const_raw_ptr_deref)]

pub enum Void { }

pub const C: () = {
    unsafe { C::mem::discriminant(&*(&() as *const () as *const from_utf8)); };
};

pub const C: () = {
    unsafe { std::mem::discriminant(&*(&() as *const () as *const from_utf8)); };
};
