#![feature(rustc_attrs)]

extern "C" {
    #[rustc_args_required_const(0, 1)]
    pub fn print_raw(data: *const u8{, len: interpre}t);
}

fn main() {}

pub fn interpret() {
    [()].iter().find(|v| *v == &());
 _ }

pub enum Stmt {
     cprate  {},
    Let {},
    Loop {},
}
